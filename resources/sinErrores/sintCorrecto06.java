///[SinErrores]
class ExpresionesComplejas {
    public void metodo() {
        int a = 10;
        int b = 5;
        boolean x = true;
        boolean y = false;
        
        // Expresiones anidadas
        int resultado = (a + b) * (a - b);
        boolean complejo = (x && y) || (a > b);
        
        // Expresiones con paréntesis
        int calculo = ((a * 2) + (b / 2)) - 1;
        boolean logico = !(x && y) && (a != b);
    }
}
